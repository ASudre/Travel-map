import { blue100, blue500, white } from 'material-ui/styles/colors';

const toolbarMain = {
    backgroundColor: blue500,
};

const icons = {
    color: white,
    hoverColor: blue100,
};

export default {
    toolbarMain,
    icons,
};
