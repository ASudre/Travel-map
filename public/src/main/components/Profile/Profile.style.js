const width = 200;
const marginLeft = -(width / 2);

const style = {
    marginTop: 50,
    height: 150,
    width,
    position: 'absolute',
    left: '50%',
    marginLeft,

};

export default style;
