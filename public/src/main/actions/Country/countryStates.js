import { RECEIVE, REQUEST } from '../constants';

const countryStates = {
    REQUEST: {
        COUNTRIES: `${REQUEST}COUNTRIES`,
    },
    RECEIVE: {
        COUNTRIES: `${RECEIVE}COUNTRIES`,
    },
};

/** **********************
 * Exports              *
 ************************
 */
export default countryStates;
