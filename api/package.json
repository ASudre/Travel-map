{
    "name": "travelmap.com",
    "version": "1.0.0",
    "description": "",
    "main": "build/server.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "rm -rf build && mkdir build && ../node_modules/.bin/babel . --ignore node_modules --out-dir build",
        "start": "node build/server.js",
        "watch": "../node_modules/.bin/babel-watch ./server.js -w ."
    },
    "keywords": [],
    "engines": {
        "node": "7.7.4"
    },
    "author": "",
    "license": "ISC",
    "babel": {
        "presets": [
            "env",
            "es2017"
        ],
        "plugins": [
            "babel-plugin-inline-import"
        ],
        "ignore": [
            "*/node_modules/**/*"
        ]
    }
}
